<UserControl x:Class="kp.Views.Users.EditUserView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <TextBox Grid.Row="0" Text="{Binding Login}" />
        <StackPanel Grid.Row="1" VerticalAlignment="Bottom" HorizontalAlignment="Right" Orientation="Horizontal">
            <Button Margin="5" Content="Apply" Command="{Binding Apply}" Style="{StaticResource MaterialDesignRaisedAccentButton}" />
            <Button Margin="5" Content="Cancel" Command="{Binding Cancel}" />
        </StackPanel>
    </Grid>
</UserControl>