<UserControl x:Class="kp.Views.Login.Login" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:ns0="clr-namespace:kp.Resources" xmlns:ns1="clr-namespace:MaterialDesignThemes.Wpf;assembly=MaterialDesignThemes.Wpf" xmlns:ns2="clr-namespace:AmmySidekick;assembly=AmmySidekick" ns2:Ammy.Register="/kp;component/Views/Login/Login.g.xaml">
  <Grid>
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="*"/>
      <ColumnDefinition Width="2*"/>
      <ColumnDefinition Width="*"/>
    </Grid.ColumnDefinitions>
    <StackPanel Grid.Column="1" VerticalAlignment="Center">
      <TextBox Style="{StaticResource MaterialDesignFloatingHintTextBox}" ns1:HintAssist.Hint="{x:Static ns0:Texts.Login}">
        <TextBox.Text>
          <Binding Path="Login"/>
        </TextBox.Text>
      </TextBox>
      <TextBox Style="{StaticResource MaterialDesignFloatingHintTextBox}" ns1:HintAssist.Hint="{x:Static ns0:Texts.Password}">
        <TextBox.Text>
          <Binding Path="Password"/>
        </TextBox.Text>
      </TextBox>
      <Button HorizontalAlignment="Center" Margin="15" Style="{StaticResource MaterialDesignRaisedAccentButton}" Content="{x:Static ns0:Texts.SignIn}"/>
    </StackPanel>
  </Grid>
</UserControl>